# 安全策略

**简体中文** | [English](SECURITY_EN.md)

## 支持范围

| 版本 | 安全修复 |
|---|---|
| `0.2.x` Beta / `main` | 支持 |
| `< 0.2` | 不支持 |

Beta 版本可能调整数据格式，但密钥处理、图片解析、分享/导入边界和第三方 Base URL
隔离仍按安全问题处理。

## 私下报告漏洞

请不要用公开 Issue 报告尚未修复的漏洞，也不要附上真实 API Key、人物原图或其他
敏感数据。

1. 在仓库的 **Security → Advisories** 页面选择 **Report a vulnerability**。
2. 如果该入口暂未启用，请通过仓库所有者 GitHub 主页提供的私密联系方式报告，
   只先发送影响范围和最小化复现说明。
3. 提供受影响版本/提交、攻击前提、影响、最小复现步骤和建议修复；凭据一律使用
   明显无效的占位符，图片使用自制或授权样本。

维护者会尽量在 7 天内确认收到，在复现和确定修复窗口后协调披露。实际时限取决于
影响和修复复杂度；在修复可用前请勿公开利用细节。

## 特别关注的边界

- API Key、错误日志和自定义 OpenAI / Anthropic Base URL 的来源隔离；
- 恶意或超大图片、EXIF、备份 JSON、网格工程、分享 URL 和 Markdown/PDF 导出；
- SQLite 历史文件的本地访问边界；
- 提示词或模型响应绕过 Pydantic、针数代数、塑形和连接校验；
- 依赖、CI 工作流、发行 wheel 与包内数据被篡改。

一般使用问题、非敏感缺陷和功能建议请使用普通 Issue 表单。
